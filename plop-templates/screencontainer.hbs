import React from 'react';
import PropTypes from 'prop-types';

import {{name}}Screen from './{{name}}Screen';

function {{name}}ScreenContainer(props) {
  const _props = {
  };

  const methods = {};

  return <{{name}}Screen {...{ ..._props, ...methods }} />;
}

{{name}}ScreenContainer.propTypes = {
  navigation: PropTypes.object,
};

export default {{name}}ScreenContainer;
